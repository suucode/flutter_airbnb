import 'package:flutter/material.dart';

class CommmonFormField extends StatelessWidget {
  const CommmonFormField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
